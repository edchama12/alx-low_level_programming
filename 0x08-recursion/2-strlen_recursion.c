#include "main.h"
#include "string.h"
#include <stdio.h>
/**
 * _strlen_recursion - function that returns the length of a string
 * @s: string 
 * Return: lenght
 */
int _strlen_recursion(char *s)
{
	int L;

	L = strlen(s);
	return (L);
}
