#include "main.h"
#include "string.h"
#include <stdio.h>
/**
 * _strlen_recursion -  returns the length of a string
 * @s: string
 * Return: intger
 */
int _strlen_recursion(char *s)
{
	int L;

	L = strlen(s);
	return (L);
}
