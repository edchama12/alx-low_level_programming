#include <stdio.h>
/**
 * main - start point
 * @argc: number of command line
 * @argv: array list all argument
 * Return: 0 if it work
 */
int main (int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
