#include <stdio.h>
#include "main.h"
/**
 * add - make sum of two numbers
 * @int: the first number
 * @int; the second number
 * Return; the result of the sum
 */
int add(int, int)
{
	return (a + b);
}
