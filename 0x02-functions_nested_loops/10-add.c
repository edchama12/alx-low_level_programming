#include <stdio.h>
#include "main.h"
/**
 * add - make sum of two numbers
 * @a: the first number
 * @b: the second number
 * Return: the result of the sum
 */
int add(int a, int b)
{
	return (a + b);
}
